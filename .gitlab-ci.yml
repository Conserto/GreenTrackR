stages:
  - build
  - test

cache:
  paths:
    - node_modules/

before_script:
  - apk add --no-cache yarn
  - yarn install --frozen-lockfile

build:
  stage: build
  script:
    - yarn build

test:
  stage: test
  script:
    - yarn test
